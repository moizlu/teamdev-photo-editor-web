# 画像エディター(Web版)

## 依存関係のインストール(初回のみ)
```sh
pnpm install # 事前にpnpmのインストールが必要です。
```

## 開発サーバーの起動(サイトの表示)
```sh
pnpm run dev       # LAN内に公開
# もしくは
pnpm run dev:local # ローカルホストのみ
```

## ビルド
```sh
pnpm run build
```
